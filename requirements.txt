# Pinned: joblib artifacts are only guaranteed to load under the library versions
# they were fitted with. Bump these and re-run train_models.py together.
Flask>=3.0.0
Flask-CORS>=4.0.0
gunicorn>=21.2.0
pandas==2.2.3
numpy==2.2.1
joblib==1.4.2
scikit-learn==1.6.0
lightgbm==4.7.0
